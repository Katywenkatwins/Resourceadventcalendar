# Система авторизації та оплати - 24 кроки до нового себе

## Огляд реалізації

Успішно впроваджено повноцінну систему авторизації, тарифів та оплати для адвент-календаря трансформації.

## Основні компоненти

### 1. Backend API (Supabase + Hono)
**Файл:** `/supabase/functions/server/index.tsx`

**Маршрути:**
- `POST /signup` - Реєстрація нового користувача
- `POST /confirm-payment` - Підтвердження оплати після WayforPay
- `GET /profile` - Отримання профілю користувача
- `POST /progress` - Оновлення прогресу по днях
- `GET /admin/users` - Отримання всіх користувачів (тільки для адміна)
- `PUT /admin/users/:userId` - Оновлення даних користувача (адмін)
- `DELETE /admin/users/:userId` - Видалення користувача (адмін)

### 2. Компоненти Frontend

#### AuthPage (`/components/AuthPage.tsx`)
- Вкладки для входу і реєстрації
- Інтеграція з Supabase Auth
- Валідація форм
- Автоматичний вхід після реєстрації

#### PricingPage (`/components/PricingPage.tsx`)
- Відображення 3 тарифів: Світло (€10), Магія (€35), Чудо (€100)
- Детальний опис переваг кожного тарифу
- Інтеграція з WayforPay (demo режим)
- Анімовані картки тарифів

#### AdminPanel (`/components/AdminPanel.tsx`)
- Перегляд всіх користувачів
- Редагування даних користувача (ім'я, тариф, статус оплати)
- Видалення користувачів
- Доступ тільки для адміністратора (Katywenka@gmail.com)

#### Оновлені компоненти
- **App.tsx** - Головний роутинг з перевіркою авторизації
- **CalendarView.tsx** - Додано інформацію про користувача, кнопки виходу та адмін-панелі
- **DayContent.tsx** - Підтримка різних тарифів

## Структура тарифів

### 💫 Світло (Basic) - €10
- Доступ до всіх 24 "дверцят"
- Відео, практики, матеріали
- 25 днів доступу
- Особистий прогрес-бар
- Email-нагадування
- PDF-гайд "План оновлення на 2026"

### 🔥 Магія (Deep Journey) - €35
Все з Basic +
- 4 спільні ефіри з експертами
- Доступ до чату учасниць
- Медитації в аудіо форматі
- Міні-чеклісти у PDF
- 3 бонусні дні після 24-го
- Доступ до записів ефірів 1 місяць

### 🌟 Чудо (Premium Experience) - €100
Все з попередніх +
- Персональна консультація від топ-експерта
- Закрита зустріч після марафону
- Lifetime доступ до марафону
- Ексклюзивні розширені матеріали
- Ім'я в розділі Ambassadors
- Спеціальний подарунок від організаторки

## Потік користувача

### Для нових користувачів:
1. Заходять на сайт → бачать лендінг
2. Натискають "Розпочати подорож"
3. Переходять на сторінку авторизації
4. Реєструються або входять
5. Автоматично переходять на сторінку вибору тарифу
6. Обирають тариф → оплата через WayforPay
7. Після оплати → доступ до календаря

### Для існуючих користувачів (оплатили):
1. Заходять на сайт
2. Автоматично переходять до календаря
3. Бачать свій прогрес та тариф

### Для адміністратора:
1. Входить через звичайну форму
2. У календарі бачить кнопку "Налаштування" (Settings)
3. Може переглядати та управляти користувачами

## База даних (KV Store)

Структура даних користувача:
```typescript
{
  id: string;                    // UUID від Supabase Auth
  email: string;
  name: string;
  tier: 'basic' | 'deep' | 'premium';
  payment_status: 'pending' | 'paid';
  progress: number[];            // Масив виконаних днів [1, 2, 3, ...]
  created_at: string;            // ISO timestamp
  payment_date?: string;         // ISO timestamp
  transaction_id?: string;       // ID транзакції WayforPay
  last_activity?: string;        // ISO timestamp
}
```

## Адмін панель

**Доступ:** Katywenka@gmail.com / Twins1650

**Можливості:**
- ✅ Перегляд списку всіх користувачів
- ✅ Редагування імені, тарифу, статусу оплати
- ✅ Перегляд прогресу кожного користувача
- ✅ Видалення користувачів
- ✅ Фільтрація за тарифом та статусом

## Інтеграція з WayforPay

**Поточний статус:** Demo режим

**Для продакшн інтеграції потрібно:**

1. Отримати credentials від WayforPay:
   - Merchant Account
   - Merchant Secret Key

2. Додати WayforPay SDK в `index.html` або використати API

3. Оновити функцію `openWayForPay` в `PricingPage.tsx`:
```typescript
const openWayForPay = (tierId, price) => {
  const wayforpay = new Wayforpay();
  wayforpay.run({
    merchantAccount: "YOUR_MERCHANT_ACCOUNT",
    merchantDomainName: "your-domain.com",
    orderReference: `ORDER_${Date.now()}`,
    orderDate: new Date().getTime(),
    amount: price,
    currency: "EUR",
    productName: [`Тариф ${getTierName(tierId)}`],
    productPrice: [price],
    productCount: [1],
    // ... інші параметри
  });
};
```

4. Налаштувати webhook для отримання callback після оплати

5. У webhook викликати `/confirm-payment` endpoint

## Безпека

✅ **Реалізовано:**
- Авторизація через Supabase Auth
- JWT токени для захисту API
- Перевірка ролі адміністратора на бекенді
- CORS налаштування
- Service Role Key залишається на сервері

⚠️ **Важливо для продакшн:**
- Налаштувати email підтвердження (зараз автоматично)
- Додати rate limiting
- Налаштувати HTTPS
- Додати логування безпеки
- Регулярні бекапи бази даних

## Тестування

### Тестові користувачі:
Створіть через форму реєстрації на сайті

### Тестування оплати:
Зараз у demo режимі - оплата симулюється автоматично після підтвердження

### Тестування адмін панелі:
1. Увійти як Katywenka@gmail.com / Twins1650
2. Натиснути іконку Settings у календарі
3. Керувати користувачами

## Наступні кроки для продакшн

1. ✅ Створити реальні credentials в WayforPay
2. ✅ Інтегрувати платіжний віджет
3. ✅ Налаштувати webhook для підтвердження оплати
4. ✅ Налаштувати email-сервіс (наприклад, Resend)
5. ✅ Додати відправку листів при реєстрації та оплаті
6. ✅ Налаштувати домен та SSL
7. ✅ Провести тестування безпеки
8. ✅ Налаштувати моніторинг та логування

## Технічні деталі

**Стек:**
- Frontend: React + TypeScript
- Backend: Deno + Hono
- Auth: Supabase Auth
- Database: Supabase KV Store
- Payment: WayforPay (інтеграція)
- UI: Tailwind CSS + shadcn/ui

**Deployment:**
- Frontend: Автоматично через Figma Make
- Backend: Supabase Edge Functions
- Database: Supabase managed

## Підтримка

При виникненні питань або проблем:
1. Перевірте логи в консолі браузера
2. Перевірте логи Supabase Edge Functions
3. Перевірте статус платежів у WayforPay dashboard
4. Зверніться до адміністратора через адмін панель
